controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute
    prefix: '%app.route_prefix%'

api_refresh_token:
  path: /api/auth/token/refresh

api_login_check:
  path: /api/user/login

